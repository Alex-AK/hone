{
  "slug": "server-runtime",
  "title": "The server runtime",
  "order": 14,
  "summary": "What the process holding your code is actually doing: how one server answers a thousand clients at once, what it does with bytes that arrive faster than it can put them somewhere, what a framework runs before your handler gets the request, and what it takes to survive the calls that fail. Node and NestJS are the worked examples, compared against the servers that solved the same problems differently."
}
